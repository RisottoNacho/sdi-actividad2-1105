# Second assingment for SDI

This app includes a normal Node Express app plus an API to use its core functionalyties only and a JQuey client
example of how to consume the app, all is in the same project

It uses a MongoDB database and includes a Selenium test case project.(It needs an empty database to work)


